# Secrets — NIE committen
orchestrator/.env
.env
# Google OAuth (Phase 11) — Client-Secret + Token nie ins Repo
client_secret*.json
**/client_secret*.json
*token*.json

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
*.egg-info/

# Logs / lokale Beobachtbarkeit
orchestrator/logs/
*.log

# Agenten-Gedaechtnis: Dry-Run-Store nicht versionieren (kanonischer log.jsonl bleibt versioniert)
orchestrator/memory/log_dryrun.jsonl

# Live-Voice: lokale Stimmauswahl (Nutzerpraeferenz, kein Versionierungsbedarf)
orchestrator/channels/voice/selected_voice.json

# Antraege: Dry-Run-Store nicht versionieren (kanonischer antraege/log.jsonl bleibt versioniert)
antraege/log_dryrun.jsonl

# Execution-Engine (Phase 7): Git-Worktrees der Antrags-Umsetzung
.worktrees/

# Build-Artefakte
.xmind-build/

# macOS
.DS_Store
**/.DS_Store
